# Dashboard-only dependencies. Deliberately separate from the repository's
# requirements.txt so the engine and the training loop stay free of them.
# 1.49 introduced width='stretch', replacing the deprecated use_container_width
streamlit>=1.49
pandas>=2.0
altair>=5.0
